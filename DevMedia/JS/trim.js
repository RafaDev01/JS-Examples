let nome = "   Diego     ";

/*
-> metodo trim() remove os espaços em brancos desnecessários mantendo apenas o conteudo da string,Geralmente o método trim() é usado no cadastro e na validação de dados 
*/

let resultado = nome.trim();

console.log(resultado);