let paises = ["Brasil", "Suíça", "França", "Japão"];

let contador = 0;

//while ira percorrer todo o array pelo .length
while ( contador < paises.length ) {

    //Acessando Array pelo indice(contador)
    console.log(paises[contador]);
    contador++;

}